<view class='card-container'>
    <view class='card-item'  wx:for="{{cardList}}" wx:key="index" bindtap='toDetail' data-id='{{item.id}}' data-name='{{item.name}}'>
        <image src='{{item.albums}}' mode='aspectFill'></image>
        <view class='name'>{{item.name}}</view>
    </view>
</view>